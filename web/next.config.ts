// FastAPI serves exported HTML pages; full-document anchors are intentional.
// The matching Next-only navigation lint rule is disabled in .oxlintrc.json.
const config = { output: 'export' };
export default config;
